// ========================================================
// SWIFLORA LOGISTICS - MAINTENANCE MODE TOGGLE
// Set to true to show a maintenance screen to every visitor instead of the
// site (public pages, admin console, customer portal — all of them). Set
// back to false to restore the site immediately. Nothing else needs to
// change; no data is touched.
// ========================================================

export const MAINTENANCE_MODE = true;
